language: java
sudo: false # faster builds
install: true

jdk:
 - oraclejdk8

script:
 - chmod +x ./gradlew build connectedCheck
 - ./gradlew build

cache:
  directories:
    - '$HOME/.gradle'
    - '.gradle'