language: java
sudo: false # faster builds
install: true

install: gradle wrapper --gradle-version 4.2

jdk:
 - oraclejdk8

script:
 - chmod +x gradlew
 - ./gradlew build

cache:
  directories:
    - '$HOME/.gradle'
    - '.gradle'