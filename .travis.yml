language: java
jdk: oraclejdk8

gradle assemble
./gradlew check

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

deploy:
  provider: pages
  skip-cleanup: true
  github-token: ceadef6bbd41ffaf966050d964f08c3317833f09
  keep-history: true
  on:
    branch: master